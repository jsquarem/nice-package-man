//const supertest = require('supertest');
const supertest = require('supertest');
const collectionController = require('../controllers/collections');

describe('collections', () => {
  describe('get collections show route', () => {
    it('should return a 404', () => {});
  });
});
